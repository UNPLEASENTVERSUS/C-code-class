#include <stdio.h>
int main()
{
    int rows, cols;
    printf("Enter number of rows & cols: ");
    scanf("%d %d", &rows, &cols);
    int arr[rows][cols];
    printf("Enter the elements in 2D Array/matrix :\n");
    loop
}